import { EmptyState } from "@/components/EmptyState";

export default function SettingsPage() {
  return <EmptyState title="Tenant settings locked in demo" description="Use seeded demo tenant values for this shell milestone." />;
}
