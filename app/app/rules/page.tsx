import { EmptyState } from "@/components/EmptyState";

export default function RulesPage() {
  return <EmptyState title="No custom automation rules" description="Seeded demo tenant has baseline SLA guardrails only." />;
}
